# Journal

Session narrative for Markdown Preview — the WHY behind changes. Newest entry first. Live state lives
in `docs/AGENT_HANDOFF.md`, not here.

When this file passes roughly 20k tokens, move older entries **unedited** to `docs/journal/` and keep
the recent few — or run `/context-diet`. Do not start a new log elsewhere; older entries are found by
`grep`.
